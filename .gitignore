# Office lock / temp files
~$*
*.tmp
*.temp
Thumbs.db
.DS_Store
Desktop.ini

# Local scratch
.venv/
.env
.env.*
!.env.example
node_modules/
dist/
.idea/
.vscode/

# Build / test artifacts
coverage/
*.log
.vitest/

# Transcript extraction scratch (regenerate from the .docx as needed)
data/_transcript_extracted.txt

# Generated pipeline output (invoices, reports) — never source; regenerate from data/
out/

# Real monthly Coro source packets — proprietary customer/financial data, NEVER commit.
# (The Sep 11 call transcript .docx is already tracked and intentionally kept.)
data/**/*.xlsx
data/**/*.xls
data/**/*.pdf
data/2026-*/
